import pygame
from pygame import *
class CharacterScreen:
    def __init__ (self, window, Character, x, y):
        window = pygame.display.set_mode([x,y])
        Character.getName() = Name
        Character.getGoal() = Goal
        print(Name)
        print(Goal)
        